---
title: Deep Shah · Designer
pageable: true
per_page: 10
---

<!-- <section>
    <h2>Hi! 🙌🏽 My name is Deep. I’m a product designer in Brooklyn. 🗽</h2>
    <p>Check out some of my work below. 👇🏽</p>
</section> -->

<section id="intro">
  <div class="row">
    <div class="column-wrap">
      <div class="column">
        <p>My name is Deep Shah, and I’m a product designer <i>(for lack of a better term)</i>. I’m currently creating solutions for developers at <a class="mongodb" href="https://www.mongodb.com/" target="_blank">MongoDB</a>.</p>
        <p>I previously designed and built software at <a class="behalf" href="https://www.behalf.com/" target="_blank">Behalf</a>, <a class="cota" href="https://www.cotahealthcare.com/" target="_blank">COTA</a>, and <a class="saavn" href="https://www.saavn.com/" target="_blank">Saavn</a>. I cut my teeth at <a class="livestream" href="https://www.livestream.com/" target="_blank">Livestream</a>.</p>
        <p>I travel to explore the world. I meditate to explore myself. I’m also a voracious reader (but not a voracious eater). <a href="/library.html">Visit</a> my virtual library for reading recommendations.</p>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="column-wrap">
      <div class="column">
        <h3 class="heading">Work</h3>
        <ul class="writing">
        <% page_articles.each_with_index do |article, i| %>
        <% if article.data.category == 'portfolio' %>
          <% if article.data.company == 'Livestream' %>
          <li><p><a class="livestream" href="<%= article.url %>"><%= article.title %></a></p></li>
          <% elsif article.data.company == 'Saavn' %>
          <li><p><a class="saavn" href="<%= article.url %>"><%= article.title %></a></p></li>
          <% elsif article.data.company == 'COTA' %>
          <li><p><a class="cota" href="<%= article.url %>"><%= article.title %></a></p></li>
          <% elsif article.data.company == 'Behalf' %>
          <li><p><a class="behalf" href="<%= article.url %>"><%= article.title %></a></p></li>
          <% elsif article.data.company == 'Otakorp' %>
          <li><p><a class="otakorp" href="<%= article.url %>"><%= article.title %></a></p></li>
          <% else %>
          <li><p><a href="<%= article.url %>"><%= article.title %></a></p></li>
          <% end %>
        <% end %>
        <% end %>
        </ul>
        <h3 class="heading">Writing</h3>
        <ul class="writing">
        <% page_articles.each_with_index do |article, i| %>
        <% if article.data.category != 'portfolio' %>
          <li><p><a href="<%= article.url %>"><%= article.title %></a></p></li>
        <% end %>
        <% end %>
        </ul>
      </div>
    </div>
  </div>
</section>

